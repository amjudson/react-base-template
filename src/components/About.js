import React, { Component, PureComponent } from 'react';

class About extends PureComponent {
  render() {
    return (
      <div>
        <h1>About</h1>
        <p>This application uses React, Redux, React Router to produce a Base Template for a React application.</p>
      </div>
    );
  }
}

export default About;
